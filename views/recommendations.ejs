<div class="recommendations-container">
  <div class="container">
    <div class="recommendations-header">
      <a href="/" class="back-btn">← Voltar</a>
      <h1>Produtos Recomendados</h1>
      <p>Com base no seu perfil de pele, aqui estão os produtos ideais para você</p>
    </div>

    <div class="profile-summary">
      <div class="profile-item">
        <strong>Tipo de Pele:</strong>
        <span><%= profile.skinType === 'oily' ? 'Oleosa' : profile.skinType === 'dry' ? 'Seca' : profile.skinType === 'combination' ? 'Mista' : 'Sensível' %></span>
      </div>
      <% if (profile.concerns && profile.concerns.length > 0) { %>
        <div class="profile-item">
          <strong>Preocupações:</strong>
          <span><%= profile.concerns.join(', ') %></span>
        </div>
      <% } %>
    </div>

    <div class="products-section fade-in-up">
      <h2>Prateleira Virtual</h2>
      <div class="products-grid">
        <% products.forEach(product => { %>
          <div class="product-card" data-product-id="<%= product.id %>" data-product-name="<%= product.name %>" data-category="<%= product.category %>">
            <div class="product-icon">
              <% if (product.category === 'cleanser') { %>
                🧴
              <% } else if (product.category === 'toner') { %>
                💧
              <% } else if (product.category === 'serum') { %>
                🧪
              <% } else if (product.category === 'moisturizer') { %>
                🧴
              <% } else if (product.category === 'sunscreen') { %>
                ☀️
              <% } else if (product.category === 'mask') { %>
                🎭
              <% } else if (product.category === 'oil') { %>
                🫒
              <% } %>
            </div>
            <h3><%= product.name %></h3>
            <p class="product-category"><%= product.category %></p>
            <% if (product.duration) { %>
              <p class="product-duration">⏱️ <%= product.duration %> min</p>
            <% } %>
          </div>
        <% }); %>
      </div>
    </div>

    <div class="action-buttons fade-in-up">
      <a href="/routine" class="btn btn-primary">
        Organizar Minha Rotina
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="9 18 15 12 9 6"></polyline>
        </svg>
      </a>
    </div>
  </div>
</div>

<style>
  .recommendations-container {
    background: linear-gradient(135deg, var(--color-secondary) 0%, var(--color-background) 100%);
    min-height: 100vh;
    padding: 40px 20px;
  }

  .recommendations-header {
    margin-bottom: 50px;
  }

  .back-btn {
    display: inline-block;
    color: var(--color-primary);
    text-decoration: none;
    margin-bottom: 20px;
    transition: all 0.3s ease;
  }

  .back-btn:hover {
    transform: translateX(-5px);
  }

  .recommendations-header h1 {
    font-family: var(--font-serif);
    font-size: 2.5rem;
    color: var(--color-foreground);
    margin-bottom: 10px;
  }

  .recommendations-header p {
    color: var(--color-muted-foreground);
    font-size: 1.1rem;
  }

  .profile-summary {
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: 15px;
    padding: 20px;
    display: flex;
    gap: 30px;
    margin-bottom: 40px;
    flex-wrap: wrap;
  }

  .profile-item {
    display: flex;
    flex-direction: column;
    gap: 5px;
  }

  .profile-item strong {
    color: var(--color-primary);
    font-size: 0.9rem;
  }

  .profile-item span {
    color: var(--color-foreground);
    font-weight: 600;
  }

  .products-section h2 {
    font-family: var(--font-serif);
    font-size: 1.8rem;
    margin-bottom: 30px;
    color: var(--color-foreground);
  }

  .products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 20px;
    margin-bottom: 40px;
  }

  .product-card {
    background: var(--color-card);
    border: 2px solid var(--color-border);
    border-radius: 15px;
    padding: 20px;
    text-align: center;
    cursor: grab;
    transition: all 0.3s ease;
    user-select: none;
  }

  .product-card:hover {
    border-color: var(--color-primary);
    box-shadow: 0 8px 24px rgba(232, 165, 194, 0.2);
    transform: translateY(-5px);
  }

  .product-card:active {
    cursor: grabbing;
  }

  .product-icon {
    font-size: 3rem;
    margin-bottom: 10px;
  }

  .product-card h3 {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-foreground);
    margin-bottom: 5px;
    line-height: 1.3;
  }

  .product-category {
    font-size: 0.85rem;
    color: var(--color-muted-foreground);
    text-transform: capitalize;
    margin-bottom: 5px;
  }

  .product-duration {
    font-size: 0.85rem;
    color: var(--color-primary);
    font-weight: 600;
  }

  .action-buttons {
    display: flex;
    justify-content: center;
    gap: 15px;
    flex-wrap: wrap;
  }

  .fade-in-up {
    animation: fadeInUp 0.6s ease-out forwards;
    opacity: 0;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .recommendations-header h1 {
      font-size: 1.8rem;
    }

    .products-grid {
      grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
      gap: 15px;
    }

    .profile-summary {
      flex-direction: column;
      gap: 15px;
    }
  }
</style>
